# Proposta FrontendV2 — Angular 21
> Data: 2026-03-12 | Versão: 1.0

---

## 1. Visão Geral

Migração e evolução do frontend React (v1) para Angular 21 standalone, mantendo a identidade visual **Swiss High Contrast** já definida em `design_guidelines.json`, adicionando gerenciamento de sessão por CNPJ via Storage e melhorando a organização de features.

---

## 2. Arquitetura Proposta

### Padrão: Feature-First + Standalone Components

```
frontendV2/src/app/
├── core/
│   ├── services/
│   │   ├── api.service.ts           # HttpClient base com interceptor de CNPJ
│   │   ├── session.service.ts       # Gerencia sessão ativa por CNPJ
│   │   └── toast.service.ts
│   ├── interceptors/
│   │   └── cnpj.interceptor.ts      # Injeta X-CNPJ header em todas as requisições
│   ├── guards/
│   │   └── session.guard.ts         # Bloqueia rotas sem CNPJ selecionado
│   └── models/
│       ├── lote.model.ts
│       ├── import-layout.model.ts
│       ├── account-mapping.model.ts
│       └── output-profile.model.ts
│
├── shared/
│   ├── components/
│   │   ├── shell/                   # Layout principal (sidebar + topbar + outlet)
│   │   │   ├── shell.component.ts
│   │   │   ├── sidebar.component.ts
│   │   │   └── topbar.component.ts
│   │   ├── stat-card/               # Card de estatística reutilizável
│   │   ├── data-table/              # Tabela genérica com paginação
│   │   ├── badge/                   # Badge de status
│   │   ├── spinner/
│   │   └── confirm-dialog/
│   └── pipes/
│       ├── cnpj.pipe.ts             # Formata CNPJ: 00.000.000/0000-00
│       └── status-label.pipe.ts
│
├── features/
│   ├── dashboard/
│   │   └── dashboard.component.ts   # Página de boas-vindas
│   │
│   └── extrato/                     # Feature principal
│       ├── extrato.routes.ts
│       ├── extrato-shell.component.ts  # Shell com cards + subnav
│       ├── upload/
│       │   └── upload.component.ts
│       ├── lotes/
│       │   ├── lotes.component.ts
│       │   └── lote-detail.component.ts
│       ├── mapeamento/
│       │   └── mapeamento.component.ts
│       ├── import-layout/
│       │   └── import-layout.component.ts
│       └── output-layout/
│           └── output-layout.component.ts
│
└── app.routes.ts
```

---

## 3. Rotas

```typescript
// app.routes.ts
[
  { path: '', redirectTo: 'dashboard', pathMatch: 'full' },
  {
    path: 'dashboard',
    loadComponent: () => import('./features/dashboard/dashboard.component')
  },
  {
    path: 'extrato',
    loadChildren: () => import('./features/extrato/extrato.routes')
  }
]

// extrato.routes.ts
[
  {
    path: '',
    component: ExtratoShellComponent,
    children: [
      { path: '', redirectTo: 'upload', pathMatch: 'full' },
      { path: 'upload',          loadComponent: ... },
      { path: 'lotes',           loadComponent: ... },
      { path: 'lotes/:id',       loadComponent: ... },
      { path: 'mapeamento',      loadComponent: ... },
      { path: 'import-layout',   loadComponent: ... },
      { path: 'output-layout',   loadComponent: ... }
    ]
  }
]
```

---

## 4. Sessão por CNPJ

### Estratégia: `SessionService` + `localStorage`

```typescript
// core/services/session.service.ts
interface CnpjSession {
  cnpj: string;
  label: string;
  setAt: string; // ISO timestamp
}

@Injectable({ providedIn: 'root' })
export class SessionService {
  private readonly STORAGE_KEY = 'ecs_active_session';
  activeSession = signal<CnpjSession | null>(null);

  // Persiste no localStorage e atualiza o signal
  setSession(session: CnpjSession): void { ... }
  clearSession(): void { ... }
  loadFromStorage(): void { ... }  // chamado no APP_INITIALIZER
}
```

- O `CnpjInterceptor` lê `sessionService.activeSession()` e injeta o header `X-CNPJ` em cada request.
- O `SessionGuard` bloqueia navegação para `/extrato/**` se não houver sessão ativa.
- O seletor de CNPJ fica no **topbar** — um dropdown que lista os CNPJs disponíveis via `GET /api/v1/import-layouts/cnpjs`.

---

## 5. Layout Visual

### 5.1 Shell Principal
- **Sidebar fixa** (esquerda, 64px/256px colapsável): Dois itens — `Dashboard` e `Extrato to TXT`.
- **Topbar sticky**: Logo + breadcrumb + seletor de CNPJ + ações globais.
- **Main area**: `<router-outlet>` centralizado com max-w-7xl.

### 5.2 Página `Extrato to TXT`
```
┌─────────────────────────────────────────────────────┐
│ [Total Lotes] [Concluídos] [Pendentes] [Processando] │  ← stat cards
├──────────────────────────────┬──────────────────────┤
│  ○ Upload   ○ Lotes          │  ⚙ ▼                 │  ← subnav + gear menu
├──────────────────────────────┴──────────────────────┤
│                                                     │
│            <router-outlet feature>                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

- **Subnav esquerdo**: `Upload` | `Lotes` (tab-style com underline ativo).
- **Botão Engrenagem (⚙)** — dropdown à direita com: `Mapeamento`, `Layout de Importação`, `Layout de Saída`.
- **Cards de estatística** buscam `GET /api/v1/lotes/estatisticas` (filtrado pelo CNPJ da sessão).

---

## 6. Design System Angular

### Tokens CSS (global styles.css)
```css
:root {
  --color-bg: #FFFFFF;
  --color-surface: #F8F9FA;
  --color-border: #E2E8F0;
  --color-primary: #0F172A;
  --color-success: #10B981;
  --color-warning: #F59E0B;
  --color-destructive: #EF4444;
  --color-info: #3B82F6;
  --font-heading: 'Manrope', sans-serif;
  --font-body: 'IBM Plex Sans', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}
```

### Convenções de nomenclatura
| Artefato        | Convenção                          | Exemplo                          |
|-----------------|------------------------------------|----------------------------------|
| Componente      | `kebab-case` pasta + `PascalCase`  | `stat-card/stat-card.component`  |
| Serviço         | `camelCase.service.ts`             | `session.service.ts`             |
| Modelo          | `camelCase.model.ts`               | `lote.model.ts`                  |
| Rota lazy       | `feature.routes.ts`                | `extrato.routes.ts`              |
| Pipe            | `kebab-case.pipe.ts`               | `cnpj.pipe.ts`                   |
| Guard           | `kebab-case.guard.ts`              | `session.guard.ts`               |
| Interceptor     | `kebab-case.interceptor.ts`        | `cnpj.interceptor.ts`            |

---

## 7. Stack Técnica

| Item               | Escolha                       |
|--------------------|-------------------------------|
| Framework          | Angular 21 (standalone)       |
| Linguagem          | TypeScript 5.9 strict         |
| Estilo             | Tailwind CSS v4 (PostCSS)     |
| Estado             | Signals + computed()          |
| HTTP               | HttpClient + interceptors     |
| Formulários        | ReactiveFormsModule           |
| Toasts             | Serviço próprio (ngx-sonner ou custom) |
| Ícones             | Lucide Angular ou SVG inline  |
| Sessão CNPJ        | localStorage + signal         |
| Change Detection   | OnPush em todos componentes   |

---

## 8. Pontos de Decisão

1. **Biblioteca de ícones**: Lucide-angular (consistência com v1) ou SVG sprite inline (zero bundle).
2. **Toast**: ngx-sonner vs. serviço Angular nativo com overlay.
3. **Tabela de lotes**: tabela HTML customizada vs. CDK Table do Angular Material (só o CDK, sem Material theme).
4. **Modal/Dialog**: Angular CDK Dialog (sem Material overhead).

---

> **Aguardando aprovação antes de iniciar implementação.**
